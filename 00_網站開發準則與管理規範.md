# 明德里 2.0 競選網站開發準則與管理規範

本文件定義競選網站的視覺設計系統、互動邏輯、目錄結構與安全性發布規範，確保多人或多個 AI 代理人接手開發時，程式碼與規格不致混亂。

---

## 1. 視覺設計系統 (Design System)

為呈現「溫馨、休閒且具未來願景」的「暖科技」質感，網頁應嚴格遵守以下視覺規範：

### 配色系統 (Color Palette)
* **主背景色**：`#060b09` (深墨綠，呈現穩重、生態休閒感)。
* **次背景色**：`#0c1411` (林蔭綠，用於卡片與區塊間隔)。
* **科技主點綴色**：`#fbbf24` (微光暖金/琥珀黃，呈現溫馨的微光與願景感)。
* **輔助點綴色**：`#10b981` (翡翠綠，用於成功標記、原則勾選)。
* **卡片背景**：採用毛玻璃質感（`rgba(12, 20, 17, 0.65)`）搭配 `backdrop-filter: blur(20px)` 及微細邊框，提升科技精緻度。
* **卡片圓角**：一律採用 `border-radius: 24px`，呈現親近、無銳角的圓潤視覺。

### 字型系統 (Typography)
* **英文與數字**：優先使用 Google Fonts 的 `Outfit` 字型，呈現現代感。
* **中文**：使用 `Noto Sans TC` (思源黑體)，確保字重清晰。

---

## 2. 互動邏輯與動畫規範

為避免頁面雜亂，動畫應維持「平滑、低干擾、高格調」的原則：

### 滾動雙向淡入淡出 (Scroll Reveal)
* **實作機制**：利用原生 JS 的 `IntersectionObserver` 監聽。
* **效果類別**：
  * `.reveal`：進入視野時，由下往上浮現並淡入。
  * `.reveal-left`：由左向右浮動淡入。
  * `.reveal-right`：由右向左浮動淡入。
* **雙向原則**：當元素**移出視野時（向上或向下滾動離開）**，必須移去 `.active` 狀態以實現淡出，確保重複滾動時依然有流暢的動態感。

### 詳情側邊欄 (Detail Drawer)
* **資料來源**：所有的政策詳細資料均保存在 `js/main.js` 中的 `POLICIES_DATA` 陣列中。
* **渲染機制**：點擊卡片時，由 JS 動態將資料填入側邊欄 DOM 中，並加上 `.active` 類別滑出。禁止使用頁面跳轉，維持單頁式應用 (SPA) 的流暢度。

---

## 3. 目錄結構與程式碼規範

網站目錄必須維持最簡結構：
```text
03_網站/
├── index.html                       # 網頁主架構與靜態佈局
├── 00_網站開發準則與管理規範.md       # 本規範文件
├── css/
│   └── style.css                    # CSS 設計變數與所有樣式定義
└── js/
    └── main.js                      # 政策資料庫與 DOM 互動邏輯
```
* **禁止 ad-hoc 內聯樣式**：所有樣式必須寫在 `style.css` 並以 CSS 變數控制。
* **資安防線**：`js/main.js` 中的政策資料僅限公開的政見精華與執行原則，**絕對不可寫入任何涉及選民個資、未公開預算分析或對手情資的敏感資料**。

---

## 4. 部署與安全發布流程

由於專案資料夾內包含高度機密檔案，部署必須採「雙軌分離」：
* **私有備份**：全專案（包含機密公文）備份至私有倉庫 [Campaign-Strategy](https://github.com/a0987183520/Campaign-Strategy.git)。
* **公開部署**：網站目錄 [03_網站](file:///d:/AI_Codex/01_AI_里長競選總部/03_網站/) 單獨備份並推送到公開的 [web](https://github.com/a0987183520/web.git) 倉庫，用於發布 GitHub Pages。
